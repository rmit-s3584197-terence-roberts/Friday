<div class="reservation new">
  <h2>Book now</h2>
</div>
<div class="row">
  <div class="col-md-6 col-md-offset-3">
    </ul>
    <%= form_for(:reservation, :url => {:action => 'create', :property_id => @property.id, :user_id => @guest.id}) do |f| %>
    <%= f.date_field :start_date %>
    <%= f.hidden_field :property_id, :value => @property.id %>
    <%= f.hidden_field :user_id, :value => @guest.id %>
    <%= f.submit "Send reservation request", class: "btn btn-primary"%>
    <% end %>
  </div>
</div>
